{
  "name": "kvuno",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed — Bootstrap served from pnpm'"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "resumablejs": "^1.1.0"
  }
}
